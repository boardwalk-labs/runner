# SPDX-License-Identifier: Apache-2.0
# Test-only stub of the `boardwalk` Python package (see _loader.py). Placed on PYTHONPATH by
# python_program.test.ts so `python -m boardwalk._loader` resolves WITHOUT the real SDK.
